object PrintAWord60times {
 // 7.Write a program to print “SEEKHO BIGDATA”for 60 times.
  def main(args: Array[String]): Unit = {
    for (i <- 1 to 60 ) {
      println("seekho bigdata")
    }
  }
}
